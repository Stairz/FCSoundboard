<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.fcsoundboard"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" /><application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        
        <!-- Splash screen -->
        <activity
            android:name="org.fcsoundboard.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
 
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main activity -->
        <activity
            android:name="org.fcsoundboard.MainActivity"
            android:label="@string/app_name" 
            android:icon="@drawable/fclogo" >
        </activity>
        
        <!-- Boma activity -->
        <activity
            android:name="org.fcsoundboard.BomaActivity"
            android:label="@string/boma_title"
            android:icon="@drawable/boma" >
        </activity>
        
    </application>

</manifest>
